# travello-backend
Backend for the Travello web application built with Node.js and Express. This repository includes all the server-side logic, database interactions, and API routes that handle requests from the frontend. The backend is connected to a database (e.g., MongoDB) and uses environment variables to manage configurations like JWT secrets and database URLs.
